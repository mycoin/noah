#includeTemplate("header")
<style>
.label {
  display: inline-block;
  width: 60px;
  text-align: right;
  margin: 0 5px;
}
button {
	margin: 10px 50px
}
</style>

<script>
	var dataList = $JsonUtils.stringify($user); 
</script>
<table>
	<tr>
		<td>
			<form action="?action=submit" method="POST" target="hiddenForm">
                <li><label class="label">userName</label><input name="userName" /></li>
                <li><label class="label">password</label><input name="password" /></li>
                <li><label class="label">status</label><input name="status" /></li>
                <button>提交</button>
            </form>
			<iframe name="hiddenForm"></iframe>
		</td>
		<td>
			<div>
			#foreach($item in $user)
				<span>$item.userName</span>
			#end
			</div>
		</td>
	</tr>
</table>
<script>
function randomString(len) {
　　len = len || 32;
　　var chars = 'ABCDEFGHJKMNPQRSTWXYZabcdefhijkmnprstwxyz2345678';
　　var maxPos = chars.length;
　　var pwd = '';
　　for (i = 0; i < len; i++) {
　　　　pwd += chars.charAt(Math.floor(Math.random() * maxPos));
　　}
　　return pwd;
}
document.getElementsByName('userName')[0].value = randomString(5)
document.getElementsByName('password')[0].value = randomString(20)
document.getElementsByName('status')[0].value = randomString(2)
</script>

$requestContext.getMessage("var.copyright") $session.id
#setLayout("layout/defaultLayout")