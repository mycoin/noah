<!-- 源码： $util.raw($search) -->
<!-- 转义： $search -->
<!-- 强转： $util.escapeInH($search) -->

#set($tpl = '<a href="OK">测试</a>')

<script data-search="$search" data-tpl="$util.escapeInHJ($content)">
    var ad = "$util.escapeInJ($content)";
    var search = "$search";

    var script = document.scripts[document.scripts.length - 1];
    var data = script.getAttribute('data-tpl');

    if(data) {
        script.insertAdjacentHTML('afterend', data);
    }
</script>

<!-- p title="$search">status-ok:$tpl</p -->

<!-- 分隔符 -->
::::    $search

<div class="c-hide">
    <!-- 分隔符 -->
    raw:    $util.raw($search)
    <!-- 分隔符 -->
    html:   $util.escapeInH($search)
    <!-- 分隔符 -->
    js:     $util.escapeInJ($search)
    <!-- 分隔符 -->
    url:    $util.escapeInU($search)
    <!-- 分隔符 -->
    htmljs: $util.escapeInHJ($search)
    <!-- 分隔符 -->

</div>
<script>
var opt = "$util.escapeInHJ($search)";
</script>