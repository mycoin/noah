<beans:beans xmlns="http://www.springframework.org/schema/security" xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:security="http://www.springframework.org/schema/security"
    xsi:schemaLocation="
    	http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">

    <beans:description>
        This file defines all of the necessary security
        configuration.

        It is split from the
        applicationContext.xml file to make
        it clearer
        which beans and configuration elements are related to
        security. It also
        makes it more reusable for other projects, as this
        file can be copied to replicate the security configuration.
    </beans:description>

	<!-- 安全配置 -->
    <http create-session="always" auto-config="true">
        <port-mappings>
            <port-mapping http="8080" https="8443" />
        </port-mappings>

        <intercept-url pattern="/sample/security*" access="ROLE_ADMIN" requires-channel="any" />
        <intercept-url pattern="/i/security/**" access="ROLE_ADMIN" requires-channel="any" />

		<!-- Configures basic auth. -->
        <http-basic />
    </http>

	<!-- 认证管理 -->
    <authentication-manager>
        <authentication-provider>
            <password-encoder hash="md5" />
            <user-service>
                <user name="admin" authorities="ROLE_ADMIN" password="21232f297a57a5a743894a0e4a801fc3" />
            </user-service>
        </authentication-provider>
    </authentication-manager>

</beans:beans>