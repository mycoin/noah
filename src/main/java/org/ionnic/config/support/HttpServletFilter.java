package org.ionnic.config.support;import java.io.IOException;import javax.servlet.FilterChain;import javax.servlet.ServletException;import javax.servlet.ServletRequest;import javax.servlet.ServletResponse;import javax.servlet.http.HttpServletRequest;import org.ionnic.config.AppConfig;import org.ionnic.config.util.ServletUtils;import org.ionnic.config.view.Context;import org.springframework.web.filter.GenericFilterBean;public class HttpServletFilter extends GenericFilterBean {	private String encoding = AppConfig.CHARSET;	private boolean forceEncoding = true;	@Override	public void doFilter(ServletRequest request, ServletResponse response, FilterChain chain) throws IOException, ServletException {		// TODO Auto-generated method stub		System.out.println("[DEBUG] org.ionnic.config.support.HttpServletFilter.doFilter(ServletRequest, ServletResponse, FilterChain)");		if (this.encoding != null && (this.forceEncoding || request.getCharacterEncoding() == null)) {			request.setCharacterEncoding(this.encoding);			if (this.forceEncoding) {				response.setCharacterEncoding(this.encoding);			}		}		chain.doFilter(request, response);	}	/**	 * @param request	 * @param statusInfo	 */	public void setAccessDenied(HttpServletRequest request) {		Context result = new Context(403, "Access Denied");		ServletUtils.setAttribute(request, result.getClass(), result);	}	/**	 * @param encoding	 *            the encoding to set	 */	public void setEncoding(String encoding) {		this.encoding = encoding;	}	/**	 * @param forceEncoding	 *            the forceEncoding to set	 */	public void setForceEncoding(boolean forceEncoding) {		this.forceEncoding = forceEncoding;	}}