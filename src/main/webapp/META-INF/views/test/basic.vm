#set($data.body = $body)
#block("tpl")
    <script type="text/javascript">
        var token = $util.toJSON($page.token);
        var token_value = '$page.token';
        var data = $util.toJSON($data);
    </script>
#end

#block("tpl")
    #if($data.body.getData().time)
        <!-- status-ok -->
    #end
#end

#external("ad", {"wd": $body.getData().app})

<!--
Request:
#foreach($item in $request.getAttributeNames())
    $item
#end
-->

<!--
Session:
#foreach($item in $session.getAttributeNames())
    $item: [$session.getAttribute($item)]
#end
-->

#request("tpl")

{
    "config": $body.status,
    "status": "$body.getData().time",
    "token": "$page.token"
}
}