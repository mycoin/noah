##setLayout()
{
    "config": "$config.get("domain")",
    "token": "$page.token"
}
#block("sce")
    <script type="text/javascript">
        var token = $util.toJSON($page.getToken());
    </script>
#end
#block("sce")
    #if($data.method && $data.controller && $data.body)
        <!-- STATUS_OK -->
    #end
#end

#external("ad", {"wd": 2})

<!--
Request:
#foreach($item in $request.getAttributeNames())
    $item
#end
-->

<!--
Session:
#foreach($item in $session.getAttributeNames())
    $item
#end
-->

