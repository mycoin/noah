{
    "config": $body.status,
    "status": $body.data.time,
    "token": "$page.token"
}

#block("sce")
    <script type="text/javascript">
        var token = $util.toJSON($page.token);
        var token_value = '$page.token';
        var data = $util.toJSON($data);
    </script>
#end


#block("sce")
    #if($body.getData().time)
        <!-- status-ok -->
    #end
#end
#external("ad", {"wd": $body.getData().app})

<!--
Request:
#foreach($item in $request.getAttributeNames())
    $item
#end
-->

<!--
Session:
#foreach($item in $session.getAttributeNames())
    $item
#end
-->