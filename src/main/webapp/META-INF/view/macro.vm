## 设置布局地址
#macro(setLayout $layoutName)
    #set($layout = "common/layout/${layoutName}.vm")
#end

## 禁用布局功能
#macro(disabledLayout)
    #set($layout = "layout.vm")
#end

## 获取内部模板
#macro(external $name, $data)#*
 *##if($data == 1)
        #parse("${name}.html")
    #else
        #set($name = "${name}.html")
        #set($name = "#include(${name})")#*
      *#$page.eval($name, $data)#*
  *##end
#end