#* -------------------------------------------- * 设置布局文件地址 * -------------------------------------------- *##macro(setLayout $layoutName)    #set($layout = "external/layout/${layoutName}.vm")#end#* -------------------------------------------- * 获取外部模板 * -------------------------------------------- *##macro(includeExternal $templateName)    #set($_ = "external/${templateName}.vm")    #set($_ = "#include($_)")    #evaluate($_)#end#* -------------------------------------------- * 注入上下文，request到工具类 * -------------------------------------------- *##macro(s $context, $request)    #end