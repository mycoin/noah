#* -------------------------------------------- * 设置布局文件地址 * -------------------------------------------- *##macro (setLayout $layoutName)    #set($layout = "layout/${layoutName}.vm")#end#* -------------------------------------------- * 获取外部模板 * -------------------------------------------- *##macro(includeExternal $templateName)    #set($_ = "external/${templateName}.vm")    #set($_ = "#parse($_)")    #evaluate($_)#end#* -------------------------------------------- * 显示javascripts * -------------------------------------------- *##macro (showJavascript)	#foreach( $script in $pageScripts )		<script src="$script"></script>	#end#end#*--------------------------------------------- * 显示stylesheets * -------------------------------------------- *##macro (showStylesheet)    #foreach( $styleSheet in $pageStyleSheets )        <link rel="stylesheet" href="$styleSheet" />    #end#end