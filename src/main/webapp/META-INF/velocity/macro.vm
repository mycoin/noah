#* -------------------------------------------- * 设置布局文件地址 * -------------------------------------------- *##macro(startup $layoutName)    #set($layout = "global/layout/${util.getValue($layoutName, 'blank')}.vm")#end#* -------------------------------------------- * 获取内部模板 * -------------------------------------------- *##macro(includeInternal $templateName)    #set($_ = "global/${templateName}.vm")    #set($_ = "#include($_)")#*  *##evaluate($_)#end#* -------------------------------------------- * 注入上下文，request到工具类 * -------------------------------------------- *##macro(s $context, $request)    #end