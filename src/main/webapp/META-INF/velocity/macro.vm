## 设置布局地址
#macro(setLayout $layoutName)
    #set($layout = "common/layout/${layoutName}.vm")
#end

## 禁用布局功能
#macro(disabledLayout)
    #set($layout = "layout.vm")
#end

## 获取内部模板
#macro(external $templateName, $data)
    #if($data == 1)
        #parse("${templateName}.html")
    #else
        #set($templateName = "#include(${templateName}.html)")
        $page.eval($templateName, $data)
    #end
#end

