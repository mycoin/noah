#* -------------------------------------------- * 设置布局文件地址 * -------------------------------------------- *##macro(extend $layoutName)    #set($layout = "common/layout/${util.getValue($layoutName, 'blank')}.vm")#end#* -------------------------------------------- * 获取内部模板 * -------------------------------------------- *##macro(includeInternal $templateName)    #set($_ = "common/${templateName}.vm")    #set($_ = "#include($_)")#*  *##evaluate($_)#end#* -------------------------------------------- * 注入上下文，request到工具类 * -------------------------------------------- *##macro(ok $context, $request)    <!-- STATUS OK -->#end